---
trigger: always_on
---

ALWAYS use PNPM instead of NPM for Node or any JS projects. NPM is slow as fuck and annoying.
PNPM is faster, uses less disk space, and handles monorepos better. It's the superior choice.